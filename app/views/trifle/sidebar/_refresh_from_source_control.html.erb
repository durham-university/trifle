<% if can?(:edit, resource) && resource.source_record.present? %>
  <li><%= link_to("Refresh from source", engine_paths.polymorphic_path([@parent,resource,:refresh_from_source].compact), method: "POST") %></li>
<% end %>
