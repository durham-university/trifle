<% content_for :sidebar do %>
  <%= render 'trifle/sidebar/open_mirador_control', resource: @resource %>
  <%= render 'trifle/sidebar/refresh_from_source_control', resource: @resource %>
  <%= render 'trifle/sidebar/publish_resource', resource: @resource %>
<% end %>

<% content_for :side_components do %>
  <%= render 'durham_rails/base_shared/show_selection' %>
<% end %>

<%= 
# next/prev probably not needed for manifests. If enabled, consider optimising
# parent.ordered_members similarly to what's done in iiif_images_controller
# render 'durham_rails/base_shared/show', next_prev: get_next_prev(method: :manifests)  
render 'durham_rails/base_shared/show'
%>
